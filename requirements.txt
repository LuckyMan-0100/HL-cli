websockets==15.0.1
asyncio==3.4.3
psycopg2-binary==2.9.9
pandas==2.2.1
numpy==1.26.4
ta-lib         # Requires C library installation (or use pandas-ta)
scikit-learn==1.4.1.post1
xgboost
lightgbm
joblib==1.3.2
gymnasium>=0.29.0
stable-baselines3[extra] # Includes extras like Optuna
pybind11       # For C++ bindings (install via pip, but used during build)
requests
python-dotenv==1.0.0
pydantic
apache-airflow  # For orchestration (consider version)
docker          # For deployment DAG
boto3           # For S3 interaction in DAGs
torch>=2.0.0
matplotlib>=3.4.0
tqdm>=4.62.0 